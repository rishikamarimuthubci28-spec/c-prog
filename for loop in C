#include <stdio.h>

int main() {
    int a, b;
    // Read the two input bounds
    if (scanf("%d\n%d", &a, &b) != 2) return 1;

    // Array to easily map numbers 1-9 to their English lowercase words
    char *words[] = {"", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Iterate through each number in the interval [a, b]
    for (int n = a; n <= b; n++) {
        if (n >= 1 && n <= 9) {
            printf("%s\n", words[n]);
        } else if (n > 9) {
            if (n % 2 == 0) {
                printf("even\n");
            } else {
                printf("odd\n");
            }
        }
    }

    return 0;
}

